---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: debian12-users
    image: debian:12
    pre_build_image: true
    command: sleep infinity

  - name: alma9-users
    image: almalinux:9
    pre_build_image: true
    command: sleep infinity

provisioner:
  name: ansible
  inventory:
    host_vars:
      debian12-users:
        ansible_user: root
      alma9-users:
        ansible_user: root
        ansible_python_interpreter: /usr/bin/python3

verifier:
  name: ansible

scenario:
  test_sequence:
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy