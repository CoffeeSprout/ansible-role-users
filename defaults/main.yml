---
# defaults file for coffeesprout.users

#Expects a user object with at least a name
users: []

#Expects a list of usernames
sudo_users: []

users_sudoersd_file: "{{ sudoersd_location }}/managed"

#users_default_key_option

#users_pubkey_location

# Documentation generation settings
doc_generate: true
doc_output_dir: "{{ playbook_dir }}/documentation"
doc_format: "both"  # Options: markdown, json, both
